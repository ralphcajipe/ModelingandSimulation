% Dimensions

vector = 1:5;
disp(vector);

% Returns the number of elements in a vector
disp(length(vector));

% Also returns the number of elements in a vector
disp(numel(vector));

% Row vector of size 1 x 5
disp(size(vector));